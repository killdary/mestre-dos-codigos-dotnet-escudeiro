namespace Questao1.Interfaces
{
    public interface IDisplay
    {

        void Divisao(decimal a, decimal b);

        void Multiplicacao(decimal a, decimal b);

        void Soma(decimal a, decimal b);

        void Subtracao(decimal a, decimal b);

        void VerificarSeEPar(decimal a, decimal b);
    }
}